 
Python FastAPI Tutorial (Part 1): Getting Started - Web App + REST API
Class 1:
Built a basic FastAPI application with multiple routes that return sample data in both HTML and JSON formats. Added automatic API documentation using /docs and /redoc, with only the API routes displayed while the HTML routes are excluded from the documentation.